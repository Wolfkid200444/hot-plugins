version = "1.0.8"
description = "Clap Clap?"

aliucord {

    changelog.set(
            """
            # 1.0.8
            * Convert from Java to Kotlin (Worst mistake ever)
            
            # 1.0.7
            * Support For V96
                
            """.trimIndent()
    )
}