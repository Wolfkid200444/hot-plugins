version = "1.0.5"
description = "Zoo"

aliucord {
    changelog.set(
            """
            # 1.0.2
            * Convert from Java to Kotlin
            
            # 1.0.1
            * Support For V96
                
            """.trimIndent()
    )
}